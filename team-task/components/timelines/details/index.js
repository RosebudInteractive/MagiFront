import React from "react";
import './details.sass'

//todo finish this
export default function TimelineDetails(props) {
    return (
        <div className="timeline-details">
            details(events and periods of timeline) here
        </div>
    )
}
