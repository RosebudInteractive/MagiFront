import { IVerticalPlaceable } from '../types/common';
declare function placeByYLevelLimit(records: IVerticalPlaceable[], limit: number, checkVisibility?: boolean): any;
export default placeByYLevelLimit;
