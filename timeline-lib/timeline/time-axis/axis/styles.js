import { StyleSheet } from 'react-native';
export default StyleSheet.create({
    wrapper: {
        borderTopColor: 'rgb(255, 255, 255)',
        borderTopWidth: 2,
        position: 'absolute',
    },
});
