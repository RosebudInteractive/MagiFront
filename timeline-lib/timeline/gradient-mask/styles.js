import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    mask: {
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        height: '100%',
        borderBottomRightRadius: 4,
    }
})
